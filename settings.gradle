rootProject.name = 'ztkyn-java'
include 'ztkyn-core'

file("${rootDir}/ztkyn-spring").eachDirMatch(~/spring.*/) {
	include "ztkyn-spring:${it.name}"
}

file("${rootDir}/ztkyn-learn").eachDirMatch(~/learn.*/) {
	include "ztkyn-learn:${it.name}"
}

